# Agent Runtime Design

The runtime is deterministic in V0.1. Agents do not call LLM APIs. Instead, each agent evaluates tasks with rule-based capabilities so the orchestration and governance logic is transparent.

