# Twitter / X Thread

1. I built Agentic Enterprise Runtime: a governed multi-agent enterprise runtime, not a chatbot.

2. It simulates 12 deterministic domain agents, 41 governed tools, 600 synthetic tasks, and 8 probability scenarios.

3. The core problem: which agent should act, which tools are allowed, what happens when agents disagree, and when should humans approve?

4. The runtime includes task routing, policy-controlled tool access, handoffs, conflict arbitration, risk simulation, approval workflows, audit logs, and scorecards.

5. V0.2 added trace-style observability, evaluation harnesses, red-team scenarios, optional live-agent adapter fallback, and a flagship demo.

6. Everything runs locally. No external API key required. Tests are deterministic.

7. Validated with 145 passing tests, ruff checks, FastAPI, Streamlit, DuckDB artifacts, and generated scorecards.

8. Repo: `<repo link>`

